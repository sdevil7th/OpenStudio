# Backward-compatible alias for the default Windows managed runtime family.
# The publishable Windows runtime is now a small base runtime; the CUDA or
# DirectML backend overlay is installed on-device during AI Tools setup.
#
# Keep this file aligned with tools/ai-runtime-requirements-windows-base.txt.
