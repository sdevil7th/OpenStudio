"""
OpenStudio-owned ACE split backend package.

This directory vendors the minimal upstream runtime needed to execute the
packaged ACE-Step split graph without relying on an external app install.
"""
